apiVersion: v1
kind: Service
metadata:
  name: airflow-dev-web
  namespace: dev
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: web
  selector:
    component: web